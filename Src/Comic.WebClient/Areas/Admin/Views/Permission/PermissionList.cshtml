@{
    Layout = "_AdminLayout";
    ViewBag.Title = "Quản lý quyền";
    ViewBag.Page = "Permission";
}

<div class="container-xxl flex-grow-1 container-p-y">

    <h4 class="mb-4">Danh sách phân quyền</h4>

    <!-- Permission Table -->
    @* <div class="card">
        <div class="row p-3">
            <div class="col-sm-12 col-md-3">
                <select id="pageSize" name="pageSize" class="form-select w-auto">
                    <option value="10">10</option>
                    <option value="25">25</option>
                    <option value="50">50</option>
                    <option value="100">100</option>
                </select>
            </div>
            <div class="col-sm-12 col-md-9">
                <div class="dt-action-buttons text-xl-end text-lg-start text-md-end text-start d-flex align-items-center justify-content-md-end justify-content-center flex-wrap me-1">
                    <div class="me-3">
                        <input type="search" id="searchTerm" name="searchTerm" class="form-control" placeholder="Nhập tên...">
                    </div>
                    <div class="dt-buttons btn-group flex-wrap">
                        <button class="btn add-new btn-primary mb-3 mb-md-0 waves-effect waves-light" tabindex="0" type="button" data-bs-toggle="modal" data-bs-target="#addPermissionModal">
                            <span>Thêm mới</span>
                        </button>
                    </div>
                </div>
            </div>
        </div>
        <div class="card-datatable table-responsive text-nowrap">
            <table id="permissionTable" class="table border-top table-hover">
                <thead>
                    <tr>
                        <th></th>
                        <th>STT</th>
                        <th>Tên quyền</th>
                        <th>Trạng thái</th>
                        <th>Tùy chọn</th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>
        </div>
        <div class="row p-3">
            <div class="col-sm-12 col-md-6">
                <div id="pageCount" role="status">
                    Hiển thị 10/1000
                </div>
            </div>
            <div class="col-sm-12 col-md-6">
                <ul class="pagination" id="pagination">
                        
                </ul>
            </div>
        </div>
    </div> *@
    <!--/ Permission Table -->

    <div class="card">
        <div class="card-datatable text-nowrap table-responsive">
            <table id="permissionDatatable" class="datatables-ajax table">
                <thead>
                    <tr>
                        <th>STT</th>
                        <th>Tên quyền</th>
                        <th>Trạng thái</th>
                        <th>Tùy chọn</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>

    <!-- Modal -->
    <!-- Add Permission Modal -->
    <div class="modal fade" id="addPermissionModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content p-3 p-md-5">
                <button type="button" class="btn-close btn-pinned" data-bs-dismiss="modal" aria-label="Close"></button>
                <div class="modal-body">
                    <div class="text-center mb-4">
                        <h3 class="mb-2">Thêm mới quyền</h3>
                        <p class="text-muted">Các quyền có thể gán cho các vai trò</p>
                    </div>
                    <form id="addPermissionForm" class="row">
                        <div class="col-12 mb-3">
                            <label class="form-label" for="modalPermissionName">Tên quyền</label>
                            <input type="text" id="addPermissionName" name="addPermissionName" class="form-control" placeholder="Tên quyền" autofocus />
                            <ul class="text-danger validate validateName"></ul>
                        </div>
                        <div class="col-12 text-center demo-vertical-spacing">
                            <button type="submit" class="btn btn-primary me-sm-3 me-1">Tạo mới</button>
                            <button type="reset" class="btn btn-label-secondary" data-bs-dismiss="modal" aria-label="Close">Hủy</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <!--/ Add Permission Modal -->
    <!-- Edit Permission Modal -->
    <div class="modal fade" id="editPermissionModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content p-3 p-md-5">
                <button type="button" class="btn-close btn-pinned" data-bs-dismiss="modal" aria-label="Close"></button>
                <div class="modal-body">
                    <div class="text-center mb-4">
                        <h3 class="mb-2">Sửa quyền</h3>
                        <p class="text-muted">Chỉnh sửa thông tin quyền mà bạn muốn</p>
                    </div>
                    <form id="editPermissionForm" class="row">
                        <div class="col-12 mb-3"
                            <label class="form-label" for="editPermissionName">Tên quyền</label>
                            <input type="text" id="editPermissionName" name="editPermissionName" class="form-control" placeholder="Nhập tên..." autofocus />
                            <ul class="text-danger validate validateName"></ul>
                        </div>
                        <div class="col-12 text-center demo-vertical-spacing">
                            <button type="submit" class="btn btn-primary me-sm-3 me-1">Cập nhật</button>
                            <button type="reset" class="btn btn-label-secondary" data-bs-dismiss="modal" aria-label="Close">Hủy</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <!--/ Edit Permission Modal -->
    <!-- /Modal -->
</div>